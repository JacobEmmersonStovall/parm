import React from 'react';

import './react-portfolio.scss';

/* eslint-disable-next-line */
export interface ReactPortfolioProps {}

export const ReactPortfolio = (props: ReactPortfolioProps) => {
  return (
    <div>
      <h1>Welcome to react-portfolio component!</h1>
    </div>
  );
};

export default ReactPortfolio;
